
/*
    Rename this file as settings.js to enable it
    
    git will ignore the file.
    
    We will use this file to safely have differences in environments
    for collaborator
    
*/

var settings = {
    web_port:8000,
    tcp_port:8080
};

module.exports = settings;